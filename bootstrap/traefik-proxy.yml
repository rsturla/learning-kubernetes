---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik-proxy-app
  namespace: argocd
spec:
  destination:
    namespace: traefik
    server: https://kubernetes.default.svc
  project: bootstrap
  source:
    path: applications/traefik-proxy
    repoURL: https://github.com/rsturla/learning-kubernetes
    targetRevision: local-cluster-bootstrapping
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
